name: Update Sources

on:
  schedule:
    - cron: '0 */1 * * *'  # 每小时运行一次（北京时间）
  workflow_dispatch:
  push:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  update-sources:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: Process unicast source
      run: |
        echo "处理单播源..."
        python scripts/process_unicast.py
        
    - name: Process multicast source
      run: |
        echo "处理组播源..."
        python scripts/process_multicast.py
        
    - name: Merge and commit final files
      run: |
        # 设置时区为北京时间
        export TZ='Asia/Shanghai'
        
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 调用合并脚本
        python scripts/merge_m3u.py
        
        # 添加合并后的最终文件
        git add unicast.m3u multicast-r2h.m3u multicast-nofcc.m3u
        
        # 检查是否有变化
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Auto-update: 合并并更新播放列表 $(date +'%Y-%m-%d %H:%M:%S')"
          git push --force-with-lease
          echo "Changes committed and pushed"
        fi
